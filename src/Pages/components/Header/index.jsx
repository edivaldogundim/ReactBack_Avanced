import React from 'react';
import styles from './Header.module.css';

const Header = () => {
    return(
        <header>

<div className={styles.texto}>Aprendendo a programar</div>
        </header>

    );
}
export default Header;